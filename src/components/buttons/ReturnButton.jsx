const ReturnButton = ({ returnLocationLabel }) => {
  return (
    <div className="return-btn">
      <img className="return-icon" />
      {returnLocationLabel}
    </div>
  );
};

export default ReturnButton;
